# HyperMARL 微电网研究交接入口

新对话先读这两个文件：

1. [`docs/ENV_V6_HANDOFF.md`](docs/ENV_V6_HANDOFF.md)：完整研究背景、会议决策、实验时间线、Env-v6 结果和下一步。
2. [`artifacts/env_v6_swiss/manifest.json`](artifacts/env_v6_swiss/manifest.json)：当前源码、网络、报告和服务器路径的机器可读索引。

本快照对应 Git 分支 `codex/env-v6-handoff-20260726`。它只保存 Env-v6 主线所需源码、测试、选中的 Swiss 网络、关键报告和交接文档；服务器原始活跃 worktree 中的历史脏文件没有被清理或覆盖。

不要把服务器密码写入 Git。SSH 密码由用户单独提供；本快照只保留 SSH 主机和端口。
